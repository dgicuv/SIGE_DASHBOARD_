export const appConfig = {
  // Tiempo de inactividad antes de cerrar sesión automáticamente (en milisegundos)
  idleTimeoutMs: 60 * 60 * 1000, // 1 minuto — cambiar a 60 * 60 * 1000 en producción
} as const;
